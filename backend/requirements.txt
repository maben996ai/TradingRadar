# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Redis
redis[asyncio]==5.2.1

# Scheduler
apscheduler==3.10.4

# HTTP client
httpx==0.27.2

# RSS/Atom 解析（行业研报信源）
feedparser==6.0.11

# Auth
PyJWT==2.10.1
bcrypt==5.0.0

# Validation & settings
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.20

# 内容分析：YouTube 下载（运行需系统安装 ffmpeg）
yt-dlp>=2025.1.1
# 音频转文本后端（对齐 yt-dlp-x：auto 优先 mlx、回退 openai）：
#   mlx-whisper 是 Apple Silicon 专用、无 Linux wheel，无法装进 Linux 镜像，
#   故仅在 macOS 宿主直跑后端时手动 `pip install mlx-whisper`（走 Apple GPU，更快）。
#   Linux/docker 环境用跨平台回退 openai-whisper（CPU，拉入 torch）。
openai-whisper

